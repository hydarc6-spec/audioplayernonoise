{
  "name": "realtime-denoise-audio-player",
  "version": "1.0.0",
  "description": "Cross-platform audio player with real-time microphone-grade noise reduction DSP applied during playback of AMR (and other) audio files.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.12.10",
    "@ffmpeg/util": "^0.12.1"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
